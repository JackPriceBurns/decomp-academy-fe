---
id: gauntlet-mul-4
title: Multiply by 4
difficulty: 2
concepts:
  - strength-reduction
  - shift
symbol: scale
hints:
  - 4 is 2^2, so this is a shift (`slwi`).
  - Write `x * 4` and match whatever the compiler emits.
---

# Strength reduction: power-of-two multipliers

The optimizer never emits a register `mullw` when multiplying by a compile-time constant. When the constant is an exact power of two, it replaces the multiply with a logical left shift (`slwi`). The shift amount is the base-2 logarithm of the constant.

For example, multiplying by 16 (2⁴) becomes:

```
slwi    r3,r3,4
blr
```

Read the shift amount in the target assembly to determine which power of two is being multiplied.

## Your task
Write `scale` to match the target.

<!-- starter -->
```c
int scale(int x) {
    // your code here
    return 0;
}
```

<!-- solution -->
```c
int scale(int x) {
    return x * 4;
}
```
